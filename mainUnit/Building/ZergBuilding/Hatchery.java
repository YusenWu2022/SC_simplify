package SC2_simplify.mainUnit.Building.ZergBuilding;

import SC2_simplify.mainUnit.Unit.UnitType.*;

import SC2_simplify.mainUnit.Unit.ZergUnit.*;
import SC2_simplify.mainUnit.Unit.*;

public class Hatchery extends ZergBuilding {
/*
    @Override
    public Unit produce(int typeID) {
        Unit newUnit;
        switch (typeID) {// 注意这里无论如何都要生成并返回一个具体的对象（包括非法的typeID，这样就是经典的小狗变飞龙的例子
            case 1:
                newUnit = new Hydralisk(getPlacex(), getPlacey(), getSide(), 1, 50);
                break;
            
            default:
                newUnit = new Hydralisk(getPlacex(), getPlacey(), getSide(), 1, 50);// 如果非法，就会出现错误
        }
        return newUnit;

    }*/
}